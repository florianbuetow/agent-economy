# Agent Roster — maps handles to names and types
# Keys are stored at: data/keys/{handle}.key and data/keys/{handle}.pub

agents:
  platform:
    name: "Platform"
    type: "platform"
  alice:
    name: "Alice"
    type: "worker"
  bob:
    name: "Bob"
    type: "worker"
  mathbot:
    name: "Math Worker Bot"
    type: "worker"
  mathbot_openai:
    name: "Math Worker Bot (OpenAI)"
    type: "worker"
  mathbot_mistral:
    name: "Math Worker Bot (Mistral)"
    type: "worker"
  feeder:
    name: "Task Feeder"
    type: "poster"
